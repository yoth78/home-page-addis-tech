# Update Contact Information

## Goal:
Update the contact information (location, email, and phone number) in the `Contact` component.

## Changes:
- **src/components/Contact.tsx**:
  - Update email to `info@addispcb.com`.
  - Update phone number to `+251928104489`.
  - Update location to `Ayer Tena`.

## Verification:
- Run `validate_build` to ensure the project still builds correctly.
